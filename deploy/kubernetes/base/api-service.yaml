apiVersion: v1
kind: Service
metadata:
  name: agentflow-api
  labels:
    app.kubernetes.io/name: agentflow
    app.kubernetes.io/component: api
spec:
  selector:
    app.kubernetes.io/name: agentflow
    app.kubernetes.io/component: api
  ports:
    - {name: http, port: 80, targetPort: http}
